# 🚀 Инструкция по загрузке на GitHub

## Быстрая загрузка проекта в репозиторий

### Шаг 1: Инициализация Git (если еще не сделано)

```bash
cd Phone-book
git init
```

### Шаг 2: Добавление всех файлов

```bash
git add .
```

### Шаг 3: Первый коммит

```bash
git commit -m "Initial commit: ContactBook - современная телефонная книга с интерфейсом в стиле macOS"
```

### Шаг 4: Подключение к удаленному репозиторию

```bash
git remote add origin https://github.com/D4cLoves/ContactBook.git
```

Если репозиторий уже подключен, обновите URL:
```bash
git remote set-url origin https://github.com/D4cLoves/ContactBook.git
```

### Шаг 5: Переименование ветки в main (если нужно)

```bash
git branch -M main
```

### Шаг 6: Загрузка кода на GitHub

```bash
git push -u origin main
```

Если возникнет ошибка о том, что репозиторий не пустой, используйте:

```bash
git push -u origin main --force
```

⚠️ **Внимание**: `--force` перезапишет все существующие файлы в репозитории!

## После загрузки

1. Перейдите на https://github.com/D4cLoves/ContactBook
2. Убедитесь, что все файлы загружены
3. Добавьте скриншоты в папку `screenshots/`
4. Обновите README, добавив ссылки на скриншоты

## Добавление скриншотов

1. Сделайте скриншоты приложения
2. Сохраните их в папку `screenshots/` с понятными именами:
   - `main-view.png` - главный экран
   - `add-contact.png` - добавление контакта
   - `search.png` - поиск
   - и т.д.

3. Загрузите на GitHub:
```bash
git add screenshots/
git commit -m "Add screenshots"
git push
```

## Настройка репозитория на GitHub

1. Перейдите в **Settings** репозитория
2. В разделе **Pages** (опционально) настройте GitHub Pages для демо
3. Добавьте **Topics** (теги):
   - `react`
   - `typescript`
   - `vite`
   - `mantine`
   - `contact-book`
   - `phonebook`
   - `web-app`

## Обновление README со скриншотами

После добавления скриншотов обновите README.md, добавив секцию:

```markdown
## 📸 Скриншоты

![Главный экран](screenshots/main-view.png)
![Добавление контакта](screenshots/add-contact.png)
```

