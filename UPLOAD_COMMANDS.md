# 🚀 Команды для загрузки на GitHub

Скопируйте и выполните эти команды по порядку в терминале (в папке Phone-book):

```bash
# 1. Перейдите в папку проекта
cd Phone-book

# 2. Инициализируйте Git (если еще не сделано)
git init

# 3. Добавьте все файлы
git add .

# 4. Создайте первый коммит
git commit -m "Initial commit: ContactBook - современная телефонная книга с интерфейсом в стиле macOS"

# 5. Подключите удаленный репозиторий
git remote add origin https://github.com/D4cLoves/ContactBook.git

# 6. Переименуйте ветку в main
git branch -M main

# 7. Загрузите код на GitHub
git push -u origin main
```

## Если репозиторий не пустой

Если в репозитории уже есть файлы, используйте:

```bash
git push -u origin main --force
```

⚠️ **Внимание**: `--force` перезапишет все существующие файлы!

## После загрузки

1. Откройте https://github.com/D4cLoves/ContactBook
2. Убедитесь, что все файлы загружены
3. Добавьте скриншоты в папку `screenshots/`
4. Загрузите скриншоты:
```bash
git add screenshots/
git commit -m "Add screenshots"
git push
```

