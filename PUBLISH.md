# 🚀 Финальная публикация на GitHub

## Выполните эти команды по порядку:

```bash
# 1. Перейдите в папку проекта
cd Phone-book

# 2. Проверьте статус Git
git status

# 3. Добавьте все файлы (включая скриншоты)
git add .

# 4. Создайте коммит
git commit -m "Add ContactBook project with screenshots

- Modern phone book application with macOS-inspired design
- Full CRUD functionality
- Search and filter contacts
- Light/Dark theme support
- Responsive design
- Added screenshots"

# 5. Подключите удаленный репозиторий (если еще не подключен)
git remote add origin https://github.com/D4cLoves/ContactBook.git

# Или обновите URL, если уже подключен:
# git remote set-url origin https://github.com/D4cLoves/ContactBook.git

# 6. Переименуйте ветку в main
git branch -M main

# 7. Загрузите на GitHub
git push -u origin main
```

## Если репозиторий не пустой:

Если в репозитории уже есть файлы, используйте force push:

```bash
git push -u origin main --force
```

⚠️ **Внимание**: `--force` перезапишет все существующие файлы!

## После публикации:

1. Откройте https://github.com/D4cLoves/ContactBook
2. Убедитесь, что все файлы загружены
3. Проверьте, что скриншоты отображаются в README
4. Добавьте описание репозитория на GitHub
5. Добавьте Topics (теги):
   - `react`
   - `typescript`
   - `vite`
   - `mantine`
   - `contact-book`
   - `phonebook`
   - `web-app`

## Готово! 🎉

Ваш проект теперь доступен на GitHub!

